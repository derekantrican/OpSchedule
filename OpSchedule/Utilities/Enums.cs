
namespace OpSchedule.Utilities
{
    public class Enums
    {
        public enum EmployeeLocation
        {
            Cincinnati,
            Seattle
        }

        public enum Permissions
        {
            Engineer,
            Manager
        }

        public enum Edit
        {
            Overwrite,
            Delete,
            Add,
            Merge
        }
    }
}
